import React from 'react';
import Nav from './Nav';

const Header = () => (
  <>
    <header>
      <h1>Book Finder</h1>
      <button type="button">로그인</button>
    </header>
    <Nav />
  </>
);

export default Header;
